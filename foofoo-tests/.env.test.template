# foofoo-tests environment template — copy to .env.test and fill in.
# .env.test is gitignored. Never commit real keys.

# ─── MVP production (original app) ───────────────────────────────────────────
SUPABASE_URL=https://ufgfznpqixplcbhmsqqw.supabase.co        # MVP production
SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...   # required for RLS/DPDP/persona creation (MVP)

# ─── RE staging (Recommendation Engine, SCHEMA-RE-001..005) ──────────────────
SUPABASE_RE_URL=https://kwypxyqxojauhiehuirz.supabase.co     # RE staging
SUPABASE_RE_ANON_KEY=...
SUPABASE_RE_SERVICE_KEY=...     # required for RE persona creation / RLS / DPDP / seed counts

# The app uses EXPO_PUBLIC_ prefixed names; either form works (globalSetup maps them).
# EXPO_PUBLIC_SUPABASE_RE_URL=https://kwypxyqxojauhiehuirz.supabase.co
# EXPO_PUBLIC_SUPABASE_RE_ANON_KEY=...

# ─── Target selection ────────────────────────────────────────────────────────
QA_TARGET=re-staging            # re-staging | mvp-prod
