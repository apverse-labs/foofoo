// Stub Supabase client for unit tests that exercise pure logic in RE repositories.
// Integration tests use a real client configured via SUPABASE_STAGING_ANON_KEY.
export const supabase = {} as any;
export const supabaseRE = {} as any;
